int a[20];
char b, c, d;
/* this is a test */
int main(char a, int b)
{
    float c = ++0.1;
    int a = (b >> 2);
    while (a > b && b < 1)
    {
        a = ++a;
    }
    printf("%s", "abcd");
    return 0;
}